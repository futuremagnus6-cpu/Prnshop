{
  "name": "pranndhara-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && cd .. && cp -r frontend/dist backend/dist",
    "start": "cd backend && node src/server.js",
    "postinstall": "cd backend && npm install"
  }
}